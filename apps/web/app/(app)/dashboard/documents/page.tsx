import { DocumentsView } from './documents-view.tsx';

/** Documents: the embedding projection first, then the corpus itself. */
export default function DocumentsPage() {
  return <DocumentsView />;
}
