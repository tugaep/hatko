import { IngestionView } from './ingestion-view.tsx';

/** Ingestion: trigger a run, and read the history of every run before it. */
export default function IngestionPage() {
  return <IngestionView />;
}
