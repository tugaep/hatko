{
  "name": "@sorrel/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HTTP API: auth, search, grounded answers, and admin surfaces",
  "scripts": {
    "dev": "node --watch src/server.ts",
    "start": "node src/server.ts",
    "test": "node --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "@sorrel/core": "*",
    "@sorrel/shared": "*",
    "hono": "^4.13.2",
    "zod": "^4.4.3"
  }
}
