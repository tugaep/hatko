node_modules/
data/
.next/
package-lock.json

# Generated by Next, and it says so in its own header: "This file should not be
# edited". It also rewrites itself between `next dev` and `next build` (the import
# path gains and loses a `dev/` segment), so it both failed `format:check` and
# turned up as noise in unrelated diffs.
apps/web/next-env.d.ts

# Case brief and provided corpus — left exactly as supplied.
sample_dataset/
TASK.md
RULES.md
MUST-HAVE.md
EVALUATION.md
DELIVERABLES.md
TECHNICAL-REQUIREMENTS.md
bonus.md
documentation.md
